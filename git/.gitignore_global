# ~/.gitignore_global - Global git ignore patterns
# Patterns in this file will be ignored across all git repositories.
# Wired up by core.excludesfile in .gitconfig -- do not run
# `git config --global`, which writes through the symlink into the
# tracked file.

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# macOS
.DS_Store
.Spotlight-V100
.Trashes
.fseventsd

# Windows
Thumbs.db
desktop.ini
